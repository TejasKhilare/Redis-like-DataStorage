apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus # the dashboard refers to it by this uid
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 5s # the scrape interval, so $__rate_interval covers >= 4 scrapes
